{
  "name": "nexttask",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "start": "concurrently \"npm --prefix server start\" \"npm --prefix client run dev\"",
    "test": "npm run test:api && npm run test:e2e",
    "test:api": "playwright test tests/api tests/approval-api.spec.js",
    "test:e2e": "playwright test tests/smoke.spec.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "concurrently": "^9.2.1"
  }
}
